name: "Install python and dependencies"
description: "Setup uv and install project dependencies"
runs:
  using: "composite"
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: uv sync --frozen
      shell: bash